// Configuration for Framework Templatizer Extension

// Railway service endpoint (unchanged — same Railway deploy, new endpoint path)
export const API_ENDPOINT = 'https://frameworks.twain.junoleads.io';

// Endpoints
export const TEMPLATIZE_ENDPOINT = `${API_ENDPOINT}/templatize-extension`;
export const HEALTH_ENDPOINT = `${API_ENDPOINT}/health`;

// Auth
export const AUTH_ENDPOINT = 'https://auth.extensions.junoleads.io';
export const EXTENSION_NAME = 'Framework Templatizer';
